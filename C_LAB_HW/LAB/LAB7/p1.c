#include <stdio.h>
#include <stdlib.h>

#define END_MARK    -1
#define MAX_SIZE    10000

int get_kth_largest(int *nums, int k) {
    int cnt = 0, tmp,i;

    while (nums[i++] != END_MARK) cnt++;

    if (k < 1 || k > cnt) return -1;

    else {

    }

	return -1;
}